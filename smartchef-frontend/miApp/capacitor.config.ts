import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter', // Puedes cambiar esto por algo como 'com.smartchef.app'
  appName: 'miApp',
  webDir: 'www'
};

export default config;
